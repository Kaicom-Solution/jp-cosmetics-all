services:
  nginx:
    image: nginx:stable-alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./jp-cosmetics-backend:/var/www/backend:ro
      - ../juaab-web-app:/var/www/juaab-backend:ro
    depends_on:
      - frontend
      - backend
      - juaab-backend
    networks:
      - app-network
    restart: always

  frontend:
    build:
      context: ./jp-cosmetics-frontend
      args:
        - NEXT_PUBLIC_SKIP_FETCH_ON_BUILD=1
    container_name: frontend
    expose:
      - "3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_BASE_URL: https://admin.kaicombd.com/api/v1
    networks:
      - app-network
    restart: always

  backend:
    build: ./jp-cosmetics-backend
    container_name: backend
    working_dir: /var/www/backend
    volumes:
      - ./jp-cosmetics-backend:/var/www/backend
    expose:
      - "9000"
    depends_on:
      - db
    networks:
      - app-network
    restart: always

  # New JUAAB Service
  juaab-backend:
    build: ../juaab-web-app
    container_name: juaab-backend
    working_dir: /var/www/juaab-backend
    volumes:
      - ../juaab-web-app:/var/www/juaab-backend
    expose:
      - "9000"
    depends_on:
      - db
    networks:
      - app-network
    restart: always

  queue:
    build: ./jp-cosmetics-backend
    container_name: queue
    working_dir: /var/www/backend
    command: php artisan queue:work --tries=3 --timeout=90
    volumes:
      - ./jp-cosmetics-backend:/var/www/backend
    depends_on:
      - db
    networks:
      - app-network
    restart: always

  # New JUAAB Queue
  queue-juaab:
    build: ../juaab-web-app
    container_name: queue-juaab
    working_dir: /var/www/juaab-backend
    command: php artisan queue:work --tries=3 --timeout=90
    volumes:
      - ../juaab-web-app:/var/www/juaab-backend
    depends_on:
      - db
    networks:
      - app-network
    restart: always

  db:
    image: mysql:8
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: strongpassword
      MYSQL_DATABASE: jp_cosmetics
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - app-network
    restart: always

volumes:
  dbdata:

networks:
  app-network:
    driver: bridge